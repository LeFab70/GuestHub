# GuestHub Mobile (SwiftUI)

Skeleton SwiftUI app mirroring the visitor flow: language selection → blacklist check → planned/new visit → badge states → checkout.

Open the Xcode project once added and run on iOS 17+.

